# LM Studio

LM Studio provides a desktop interface for running local models and can expose a local API for applications.

Use its current desktop documentation to install the appropriate release, download a compatible model, start the local server, and copy the API endpoint into your agent configuration.

Check model architecture, quantization, memory requirements, context length, and tool-calling compatibility before selecting a model.

Official site: https://lmstudio.ai
